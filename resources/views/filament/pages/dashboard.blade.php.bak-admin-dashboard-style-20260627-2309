<x-filament-panels::page>
    <div class="space-y-6">

        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-4">
            <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
                <p class="text-sm font-bold text-gray-500">👥 Membres</p>
                <p class="text-3xl font-black mt-2">{{ number_format($usersCount, 0, ',', ' ') }}</p>
                <p class="text-xs text-gray-500 mt-1">+{{ $todayUsersCount }} aujourd’hui</p>
            </div>

            <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
                <p class="text-sm font-bold text-gray-500">📦 Annonces publiées</p>
                <p class="text-3xl font-black mt-2">{{ number_format($publishedListingsCount, 0, ',', ' ') }}</p>
                <p class="text-xs text-gray-500 mt-1">{{ number_format($totalListingsCount, 0, ',', ' ') }} au total</p>
            </div>

            <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
                <p class="text-sm font-bold text-gray-500">👀 Vues annonces</p>
                <p class="text-3xl font-black mt-2">{{ number_format($viewsCount, 0, ',', ' ') }}</p>
                <p class="text-xs text-gray-500 mt-1">{{ number_format($favoritesCount, 0, ',', ' ') }} favoris</p>
            </div>

            <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
                <p class="text-sm font-bold text-gray-500">💬 Messages</p>
                <p class="text-3xl font-black mt-2">{{ number_format($messagesCount, 0, ',', ' ') }}</p>
                <p class="text-xs text-gray-500 mt-1">+{{ $todayMessagesCount }} aujourd’hui</p>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div class="rounded-3xl bg-emerald-50 dark:bg-emerald-950/30 border border-emerald-100 dark:border-emerald-900 p-5">
                <p class="text-sm font-black text-emerald-800 dark:text-emerald-300">💳 Transactions payées</p>
                <p class="text-3xl font-black mt-2">{{ number_format($paidTransactionsCount, 0, ',', ' ') }}</p>
            </div>

            <div class="rounded-3xl bg-teal-50 dark:bg-teal-950/30 border border-teal-100 dark:border-teal-900 p-5">
                <p class="text-sm font-black text-teal-800 dark:text-teal-300">💰 Volume validé</p>
                <p class="text-3xl font-black mt-2">{{ number_format($completedAmount, 0, ',', ' ') }} €</p>
            </div>

            <div class="rounded-3xl bg-indigo-50 dark:bg-indigo-950/30 border border-indigo-100 dark:border-indigo-900 p-5">
                <p class="text-sm font-black text-indigo-800 dark:text-indigo-300">🏝️ Commission plateforme</p>
                <p class="text-3xl font-black mt-2">{{ number_format($commissionAmount, 0, ',', ' ') }} €</p>
            </div>
        </div>

        <div class="grid grid-cols-1 xl:grid-cols-2 gap-6">
            <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
                <div class="flex items-center justify-between mb-4">
                    <div>
                        <h2 class="text-xl font-black">🌍 Répartition par territoire</h2>
                        <p class="text-sm text-gray-500">Annonces publiées par île.</p>
                    </div>
                </div>

                <div class="space-y-3">
                    @forelse($territories as $row)
                        @php
                            $percent = $publishedListingsCount > 0 ? round(($row->total / $publishedListingsCount) * 100) : 0;
                        @endphp
                        <div>
                            <div class="flex justify-between text-sm font-bold mb-1">
                                <span>{{ $row->territoire ?: 'Non renseigné' }}</span>
                                <span>{{ $row->total }} · {{ $percent }}%</span>
                            </div>
                            <div class="h-3 rounded-full bg-gray-100 dark:bg-gray-800 overflow-hidden">
                                <div class="h-full bg-teal-600 rounded-full" style="width: {{ $percent }}%"></div>
                            </div>
                        </div>
                    @empty
                        <p class="text-gray-500">Aucune donnée.</p>
                    @endforelse
                </div>
            </div>

            <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
                <h2 class="text-xl font-black mb-4">🔥 Annonces les plus vues</h2>

                <div class="space-y-3">
                    @forelse($topListings as $listing)
                        <a href="{{ route('listings.show', $listing) }}" target="_blank" class="flex items-center justify-between gap-3 rounded-2xl bg-gray-50 dark:bg-gray-800 p-3 hover:bg-gray-100 dark:hover:bg-gray-700">
                            <div class="min-w-0">
                                <p class="font-black truncate">{{ $listing->title }}</p>
                                <p class="text-xs text-gray-500">{{ $listing->user->name ?? 'Utilisateur' }}</p>
                            </div>
                            <span class="shrink-0 text-sm font-black">👀 {{ (int) $listing->views_count }}</span>
                        </a>
                    @empty
                        <p class="text-gray-500">Aucune annonce.</p>
                    @endforelse
                </div>
            </div>
        </div>

        <div class="rounded-3xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 p-5 shadow-sm">
            <h2 class="text-xl font-black mb-4">🧾 Dernières transactions</h2>

            <div class="overflow-x-auto">
                <table class="w-full text-sm">
                    <thead>
                        <tr class="text-left text-gray-500 border-b border-gray-100 dark:border-gray-800">
                            <th class="py-3">Annonce</th>
                            <th class="py-3">Acheteur</th>
                            <th class="py-3">Vendeur</th>
                            <th class="py-3">Statut</th>
                            <th class="py-3 text-right">Montant</th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($recentTransactions as $transaction)
                            <tr class="border-b border-gray-50 dark:border-gray-800">
                                <td class="py-3 font-bold">{{ $transaction->listing->title ?? 'Annonce supprimée' }}</td>
                                <td class="py-3">{{ $transaction->buyer->name ?? '-' }}</td>
                                <td class="py-3">{{ $transaction->seller->name ?? '-' }}</td>
                                <td class="py-3">{{ $transaction->status }}</td>
                                <td class="py-3 text-right font-black">{{ number_format($transaction->amount, 0, ',', ' ') }} €</td>
                            </tr>
                        @empty
                            <tr>
                                <td colspan="5" class="py-6 text-center text-gray-500">Aucune transaction.</td>
                            </tr>
                        @endforelse
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</x-filament-panels::page>
