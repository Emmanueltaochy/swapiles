@extends('layouts.app')

@section('title', $user->name . ' — Profil vendeur Swap\'Îles')

@section('content')

<section class="bg-white border-b border-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-5">
        <a href="{{ url()->previous() }}" class="text-sm font-semibold text-gray-500 hover:text-teal-700">
            ← Retour aux annonces
        </a>
    </div>
</section>

<section class="bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-12">

        <div class="flex flex-col lg:flex-row lg:items-start lg:justify-between gap-8">

            <div class="flex flex-col sm:flex-row gap-6 sm:items-center">

                <div class="w-32 h-32 sm:w-40 sm:h-40 rounded-full bg-teal-100 flex items-center justify-center overflow-hidden text-6xl font-extrabold text-teal-800 shrink-0">
                    @if($user->avatar)
                        <img src="{{ $user->avatar }}" alt="{{ $user->name }}" class="w-full h-full object-cover">
                    @else
                        {{ strtoupper(substr($user->name, 0, 1)) }}
                    @endif
                </div>

                <div>
                    <h1 class="text-4xl sm:text-5xl font-extrabold text-gray-900">
                        {{ $user->name }}
                    </h1>

                    <div class="mt-3 space-y-2 text-gray-600">
                        <p class="text-sm">
                            Membre de Swap'Îles
                        </p>

                        @if($user->territoire)
                            <p class="text-sm">
                                📍 {{ $user->territoire }}
                            </p>
                        @endif

                        <p class="text-sm">
                            ⭐ <span class="font-bold text-gray-900">{{ number_format((float) $user->rating, 1, ',', ' ') }}</span>
                            · Vendeur fiable
                        </p>
                    </div>

                    <div class="mt-6 grid grid-cols-3 gap-5 sm:gap-8 text-center sm:text-left">
                        <div>
                            <p class="text-2xl font-extrabold text-gray-900">{{ $user->transactions_count ?? 0 }}</p>
                            <p class="text-xs text-gray-500">Transactions</p>
                        </div>

                        <div>
                            <p class="text-2xl font-extrabold text-gray-900">{{ $listings->total() }}</p>
                            <p class="text-xs text-gray-500">Annonces</p>
                        </div>

                        <div>
                            <p class="text-2xl font-extrabold text-gray-900">{{ $reviewsCount }}</p>
                            <p class="text-xs text-gray-500">Avis</p>
                        </div>
                    </div>
                </div>

            </div>

            <div class="flex gap-3">
                @if(auth()->check() && auth()->id() !== $user->id)
                    <a href="{{ route('account.messages.show.general', $user) }}"
                       class="px-6 py-3 rounded-2xl border border-gray-300 text-gray-700 font-bold hover:bg-gray-50">
                        💬 Message
                    </a>
                @elseif(!auth()->check())
                    <a href="{{ route('login') }}"
                       class="px-6 py-3 rounded-2xl border border-gray-300 text-gray-700 font-bold hover:bg-gray-50">
                        💬 Message
                    </a>
                @endif

                <button class="px-6 py-3 rounded-2xl bg-teal-700 text-white font-bold">
                    ♡ Suivre
                </button>
            </div>

        </div>

    </div>
</section>

<section class="bg-white border-t border-gray-100">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex gap-8">
            <a href="{{ route('profiles.show', $user) }}"
               class="py-4 font-extrabold {{ $activeTab === 'annonces' ? 'border-b-4 border-teal-700 text-teal-700' : 'text-gray-400' }}">
                Annonces
            </a>

            <a href="{{ route('profiles.show', ['user' => $user, 'tab' => 'about']) }}"
               class="py-4 font-extrabold {{ $activeTab === 'about' ? 'border-b-4 border-teal-700 text-teal-700' : 'text-gray-400' }}">
                À propos
            </a>

            <a href="{{ route('profiles.show', ['user' => $user, 'tab' => 'reviews']) }}"
               class="py-4 font-extrabold {{ $activeTab === 'reviews' ? 'border-b-4 border-teal-700 text-teal-700' : 'text-gray-400' }}">
                Avis
            </a>
        </div>
    </div>
</section>

<section class="bg-gray-50 min-h-screen py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        @if($activeTab === 'about')
            <div class="bg-white rounded-3xl border border-gray-100 shadow-sm p-6 max-w-3xl">
                <h2 class="text-2xl font-extrabold text-gray-900">À propos de {{ $user->name }}</h2>

                <div class="mt-5 space-y-3 text-gray-600">
                    <p>📍 Territoire : <span class="font-bold text-gray-900">{{ $user->territoire ?? 'Non renseigné' }}</span></p>
                    <p>📅 Membre depuis : <span class="font-bold text-gray-900">{{ $user->created_at->format('m/Y') }}</span></p>
                    <p>📦 Annonces en ligne : <span class="font-bold text-gray-900">{{ $listings->total() }}</span></p>
                    <p>⭐ Note vendeur : <span class="font-bold text-gray-900">{{ number_format((float) $user->rating, 1, ',', ' ') }}</span></p>
                </div>
            </div>
        @elseif($activeTab === 'reviews')
            <div class="bg-white rounded-3xl border border-gray-100 shadow-sm p-6 max-w-3xl">
                <h2 class="text-2xl font-extrabold text-gray-900">Avis</h2>

                @forelse($reviews as $review)
                    <div class="mt-5 border-t border-gray-100 pt-5">
                        <p class="font-bold text-gray-900">
                            ⭐ {{ number_format((float) ($review->rating ?? 0), 1, ',', ' ') }}
                        </p>
                        <p class="text-gray-600 mt-2">
                            {{ $review->comment ?? 'Aucun commentaire.' }}
                        </p>
                    </div>
                @empty
                    <div class="mt-6 rounded-3xl bg-gray-50 border border-dashed border-gray-200 p-8 text-center">
                        <div class="text-5xl mb-3">⭐</div>
                        <p class="font-bold text-gray-900">Aucun avis pour le moment</p>
                        <p class="text-gray-500 mt-1">Les avis apparaîtront après les transactions.</p>
                    </div>
                @endforelse
            </div>
        @else

        <div class="flex items-center justify-between mb-6">
            <h2 class="text-xl sm:text-2xl font-extrabold text-gray-900">
                {{ $listings->total() }} annonces en ligne
            </h2>

            <span class="hidden sm:inline-flex px-4 py-2 rounded-full bg-white border border-gray-200 text-sm font-semibold text-gray-600">
                Trier par : Récentes
            </span>
        </div>

        <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-3 sm:gap-5">
            @forelse($listings as $listing)
                <a href="{{ route('listings.show', $listing) }}" class="group bg-white rounded-2xl overflow-hidden border border-gray-100 shadow-sm hover:shadow-xl transition">
                    <div class="relative aspect-[3/4] bg-gray-100 overflow-hidden">
                        @if($listing->images->first())
                            <img src="{{ $listing->images->first()->url }}" alt="{{ $listing->title }}" loading="lazy" class="w-full h-full object-cover group-hover:scale-105 transition duration-500">
                        @else
                            <div class="w-full h-full flex items-center justify-center text-gray-300 text-5xl">📦</div>
                        @endif

                        <span class="absolute top-2 right-2 w-9 h-9 rounded-full bg-white/90 flex items-center justify-center shadow text-gray-500">
                            ♡
                        </span>
                    </div>

                    <div class="p-3">
                        <p class="text-sm font-bold text-gray-900 line-clamp-1">{{ $listing->title }}</p>

                        @if($listing->user)
                            <p class="text-xs text-gray-500 mt-1 line-clamp-1">
                                Vendu par <span class="font-semibold text-gray-700">{{ $listing->user->name }}</span>
                            </p>
                        @endif

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">
                            @if($listing->taille){{ strtoupper($listing->taille) }}@endif
                            @if($listing->etat) · {{ $listing->etat }}@endif
                        </p>

                        <p class="font-extrabold text-gray-900 mt-2">
                            @if($listing->price > 0)
                                {{ number_format($listing->price, 0, ',', ' ') }} €
                            @else
                                <span class="text-green-600">Gratuit</span>
                            @endif
                        </p>
                    </div>
                </a>
            @empty
                <div class="col-span-full bg-white border border-dashed border-gray-300 rounded-3xl p-10 text-center">
                    <div class="text-5xl mb-3">🌴</div>
                    <h3 class="text-xl font-bold text-gray-900">Aucune annonce en ligne</h3>
                </div>
            @endforelse
        </div>

        <div class="mt-10">
            {{ $listings->links() }}
        </div>

        @endif

    </div>
</section>

@endsection
